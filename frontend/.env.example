# Examples for the private .ops/.env; do not create a frontend .env.
# Optional public API override; omit to use the development proxy.
# VITE_EDEN_API_URL=http://127.0.0.1:8000/v1

# Server-only settings. Prefer the .ops/api_and_client.sh launcher.
# EDEN_EXPLORER_UPSTREAM=http://127.0.0.1:8000
# EDEN_EXPLORER_TOKEN=match-the-local-api-token
