# A throttled-but-alive API is worse than a killed-and-restarted one:
# no soft limit (MemoryHigh throttling froze the API on 2026-09-15), a hard cap
# sized for the API alone, no swap, always restart.
[Service]
Restart=always
MemoryHigh=infinity
MemoryMax=400M
MemorySwapMax=0
TimeoutStopSec=20
