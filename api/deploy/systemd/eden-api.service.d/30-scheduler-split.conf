# The public API no longer hosts the scheduler; eden-scheduler.service does.
# Wants= makes `systemctl start eden-api` start the scheduler as well, and the
# later EnvironmentFile overrides the shared SCHEDULER_ENABLED for this unit only.
[Unit]
Wants=eden-scheduler.service

[Service]
EnvironmentFile=/opt/eden/shared/api.env
