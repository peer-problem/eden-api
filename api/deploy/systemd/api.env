# Installed as /opt/eden/shared/api.env (root:eden, 0640). Applies to eden-api.service
# only; the shared /opt/eden/shared/.env keeps SCHEDULER_ENABLED=true for
# eden-scheduler.service and the soak timers.
SCHEDULER_ENABLED="false"
